name: GitHub-Profile-Summary-Cards

on:
  schedule: # execute every 24 hours
    - cron: "* */24 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate

    steps:
      - uses: actions/checkout@v2
      - uses: vn7n24fzkq/github-profile-summary-cards@release
        env:
          GITHUB_TOKEN: 3cfeb59f8872884901b177ffdbd904752e1c8d50 # ${{ secrets.GITHUB_TOKEN }}
        with:
          USERNAME: atom-tr # ${{ github.repository_owner }}
